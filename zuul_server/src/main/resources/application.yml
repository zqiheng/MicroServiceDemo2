eureka:
  client:
    service-url:
      defaultZone: http://localhost:10001/eureka/            # 指定注册中心

server:
  port: 99  # 服务自己的访问端口

spring:
  application:
    name: zuul-server  # 服务名

# 自定义路由规则
#zuul:
#  routes:
#    service-product: /product/**
#    service-user: /user/**
#    service-order: /order/**
#  # 忽略service-*的方式访问
#  # ignored-patterns: /service-*/**
#  # 忽略整个服务，不对外提供接口
#  #ignored-services: service-order


